@model GameReviews.Models.Game

@{
    ViewBag.Title = "Home Page";
}

<div class="containing-div">
    <img src="@Model.PictureURL" class="home-img" />

    <div class="home-image-overlay">
        <img src="~/Images/logo.png" />
        <h2>Welcome to Game hut</h2>
        <div class="browse-btn-container">
            <a href="/Games" class="browse-btn">
                <span>BROWSE NOW</span>
            </a>

        </div>
    </div>

</div>

<script>

</script>

<style>
    :root {
        --color-dark: rgba(23, 26, 33, 1);
        --color-blank: rgba(199, 213, 224, 1);
        --color-accent: rgba(102, 192, 244, 1);
        --color-standard: rgba(27, 40, 56, 1);
        --color-standard-pale: rgba(42, 71, 94, 1);
    }

    * {
        color: var(--color-blank);
    }

    .home-img {
        opacity: 0.25;
        width: 98%;
        filter: blur(1rem);
        margin: 1em 0;
    }

    .containing-div {
        background-color: var(--color-dark);
        border-radius: 0 0 1em 1em;
        position: relative;
        display: flex;
        flex-direction: column;
        align-content: center;
        align-items: center;
    }

    .home-image-overlay {
        width: 100%;
        display: flex;
        align-content: center;
        align-items: center;
        text-align: center;
        flex-direction: column;
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
    }

        .home-image-overlay > img {
            display: block;
            width: 70%;
            margin-top: 5%;
        }

        .home-image-overlay > h2, .home-description {
            font-family: 'Poppins';
            font-weight: 900;
        }

    .browse-btn {
        text-decoration: none;
        padding: 0.5em 1em;
        transition: 0.3s ease-in;
    }

        .browse-btn:hover {
            text-decoration: none;
            border-bottom: 0.2em solid var(--color-blank);
            box-shadow: 0 2em 3em var(--color-accent);
        }
</style>